# OMC tool workspace
.omc/

# Claude Code アクセス制御設定（リポジトリには含めない）
.claudeignore

# node
node_modules/

# 機密ファイル（絶対にコミットしない）
.env
.env.*
*.pem
*.key
*.secret

# Dashboard logs
dashboard/*.log
# 監視Worker手動再点検トークン（絶対にコミットしない）
dashboard/.monitor-token
# 週次整頓チェックの結果（ローカル環境の状態・コミットしない）
dashboard/tidy-report.json

# 内部用 AI プロジェクトメモ（絶対に commit しない）
CLAUDE.md
.claude/
.cursorrules

# 内部メモ系（コミット禁止）
docs/claude-code-handoff-*.md
docs/weekly-review-*.md
docs/handoff-*.md
docs/internal-*.md

# セッション運用ファイル（Cowork ↔ Claude Code 連携、commit 禁止）
.session/

# Cloudflare Wrangler 関連の機密キャッシュ（厳重・どの階層でも無視）
# ※ スラッシュを含まないパターンは全階層にマッチするため、これだけで網羅できる
**/.wrangler/
wrangler-account.json
cf.json
*.bak

# Codex / Cursor agent のローカルキャッシュ (hooks.json が絶対パスでチーム移植不可)
.codex/

# 作業用スクラッチ（県突合の元XLSX等・git管理外の参照データ。deploy の untracked ガードに引っかかるため無視。rsync 対象外なので本番には流れない）
tmp/
_dupbackup_20260702/
AGENTS.md.bak-*
CLAUDE.md.bak-*
重複ファイル整理手順.md
.DS_Store
dashboard/index.html
